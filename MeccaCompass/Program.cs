namespace MeccaCompass;

// Empty class with just a static main method
// In MAUI apps, the actual entry point is handled by the platform-specific code
public static class Program
{
    // This is just a placeholder to satisfy the compiler that needs a Main method
    public static void Main()
    {
        // The MauiProgram.CreateMauiApp() method is used by the platform-specific entry points
    }
}